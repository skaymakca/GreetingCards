# Python
.venv/
__pycache__/
*.pyc

# Dev-mode data (preferences, database)
.local/

# macOS
.DS_Store

# Build artifacts
_build/
dist/

# Type checkers
.mypy_cache/

# Claude Code
.claude/
.mcp.json

# IDE / Inspections
Inspections/

# Testing
.pytest_cache/
.coverage
*.cover
.tox/
*.log

# Build script output (.tsv is committed; .tsv.gz only in runtime_content)
content/data/family_name_database.tsv.gz

# Secrets & credentials (defensive — prevent accidental commits)
*.cer
*.p12
*.p8
*.key
*.pem
*.mobileprovision
*.provisionprofile
.env
.env.*

# Sparkle framework (downloaded on demand via `make sparkle`)
packaging/Sparkle.framework/
packaging/sparkle-bin/

# Hugo build output
website/public/

# Local release configuration (generated by: make configure-release)
release-local.sh
